using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShopOverlay : MonoBehaviour
{
    // Start is called before the first frame update
    int currentID = 0;

    // Road, Village, Wall, City

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
